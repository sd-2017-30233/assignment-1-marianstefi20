package bussiness;

/**
 * Created by maria on 4/6/2017.
 * The "detective", when is called, makes a record in the database of the time and the user who initiated such an action
 * Each action calls the detective too, so at the end, each user will have a set with all of his actions in the context
 * of the application
 */
public class Detective {
    public void commit(UserController user, ClientController client) {

    }
}
